name: Deploy (placeholder)

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deployment target not configured
        run: |
          echo "No deployment target configured yet."
          echo "Add steps here for Azure/AWS/Docker/IIS when ready."
